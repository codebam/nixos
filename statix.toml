# hardware-configuration.nix files are generated by nixos-generate-config and
# carry a "Do not modify this file!" header, so their lints are not actionable.
ignore = ["*/hardware-configuration.nix"]
